#include "MatrixModel.h"

bool MatrixModel::setWeights() {

    return true;
}

std::vector<std::vector<double>> MatrixModel::feedForward(std::vector<std::vector<double>> &input_layer) {

    return std::vector<std::vector<double>>();
}

void MatrixModel::backPropagation(std::vector<std::vector<double>> &output_layer) {
    
}

bool MatrixModel::updateWeights() {

    return true;
}
